# Neural_Network
This is a simple repositary to build a Multi Layer Perceptron From Scratch. We will build the Actual Computation Graph from the Nodes up, we will implement backpropagation. We will create the loss function and train a POC MLP on fake data by hand
