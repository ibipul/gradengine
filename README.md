# A Neural Network Playground
 - We will define Nodes of a computation graph 
 - We will build the Actual Computation Graph on 
 - We will implement backpropagation on the graph 
 - We will codify a simple neuron
 - We will build layers and then finally a MLP
